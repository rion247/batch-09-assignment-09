import { Helmet } from 'react-helmet';

const LoginPage = () => {
    return (
        <div>

            <Helmet>
                <title>Modern Living | Login Page</title>
            </Helmet>

            <h1>Login Page</h1>

        </div>
    );
};

export default LoginPage;