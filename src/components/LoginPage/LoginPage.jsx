import { Helmet } from 'react-helmet';

const LoginPage = () => {
    return (
        <div>

            <Helmet>
                <title>Real Estate | Login Page</title>
            </Helmet>

            <h1>Login Page</h1>

        </div>
    );
};

export default LoginPage;